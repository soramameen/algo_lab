# slides 作業ルール

## ディレクトリ構成

| パス | 役割 |
|------|------|
| `slides/*.md` | スライド本文 |
| `docs/` | 原文資料・参照用（スライド本文ではない） |
| `plans/` | 計画ファイル置き場 |
| `images/` | 図ファイル（`.mmd` と `.svg` のペアで管理） |

## 図の作成

- Mermaid（`.mmd`）で図を作成する
- `mmdc` で SVG に変換して使用する

## スライド作業

- `officecli` 経由で行う
